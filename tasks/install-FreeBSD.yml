---

- name: Install postgresql
  pkgng:
    # default php56-pgsql depends on 9.3
    name: "{{ item }}"
    state: present
  with_items:
    - postgresql93-server
    - postgresql93-client
    - py27-psycopg2
